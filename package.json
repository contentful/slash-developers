{
  "name": "slash-developers",
  "private": true,
  "devDependencies": {
    "markdown-spellcheck": "^0.10.0",
    "hercule": "^1.2.5"
  },
  "scripts": {
    "spellcheck-main": "mdspell -r -n -a --en-us docs/**/*.md; exit 0",
    "spellcheck-apiary": "mdspell -r -n -a --en-us apiary/**/*.apib; exit 0"
  }
}
